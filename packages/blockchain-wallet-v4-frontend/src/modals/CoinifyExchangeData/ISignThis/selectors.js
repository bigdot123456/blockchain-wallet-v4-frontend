// import { lift } from 'ramda'
// import { selectors } from 'data'
//
// export const getData = (state) => {
//   const profile = selectors.core.data.coinify.getProfile(state)
//   const paymentId = selectors.core.data.coinify.getLatestTradePaymentId(state)
//
//   return lift((profile, paymentId) => ({ profile, paymentId }))(profile, paymentId)
// }
